name: pastel_widget
description: A simple but useful widget for your Flutter project.
version: 0.0.1
repository: https://github.com/snowmerak/pastel_widget

environment:
  sdk: ^3.1.2

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
